<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AzureMigrateExplore.ProjectDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureMigrateExplore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="White">

    <Page.Resources>
        <!-- Define colors using SolidColorBrush -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#0078D7"/>
        <SolidColorBrush x:Key="AccentColor" Color="#32CD32"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="CardBackgroundColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#000000"/>

        <!-- Define styles -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundColor}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Shadow">
                <Setter.Value>
                    <ThemeShadow/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Empty row to push content down -->
        <Grid Grid.Row="0" Height="50"/>

        <!-- Main content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Info Bar Section -->
            <InfoBar
                Grid.Row="1"
                IsOpen="True"
                CornerRadius="10"
                Severity="Informational"
                Title="Pre-Read"
                IsClosable="False"
                Message="Please ensure the following:&#x0a;1. You have created an Azure Migrate Project.&#x0a;2. You have contributor access to the Resource Group where the Azure Migrate Project resides.&#x0a;3. You have completed the discovery of your environment by deploying an Azure Migrate appliance or the discovery of inventory hosted on VMWare using RVTools import."
                Margin="20"/>

            <!-- Form Fields in Card-Style Container -->
            <Grid Grid.Row="0" Margin="10">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock x:Name="FilesPresent" Padding="0,0,0,30" FontWeight="Medium" FontSize="13" Foreground="OrangeRed" TextWrapping="Wrap"/>
                        
                        <TextBlock Text="Project Details" Padding="0,0,0,30" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryColor}"/>

                        <Grid>
                            <Grid.RowDefinitions> 
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Tenant ID Field -->
                            <TextBlock Text="Select the tenant Id:" FontWeight="Bold" Margin="0,0,0,0" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                            <TextBox x:Name="TenantIdEntry" Background="White" Foreground="#000000" Width="360" Grid.Row="0" Grid.Column="1" Margin="0,0,0,0" IsReadOnly="False" IsEnabled="True" TextChanged="TenantIdEntry_TextChanged"/>
                            <Button x:Name="ConfirmTenantIdChangeButton" Content="Change Tenant" Click="ConfirmTenantIdChangeButton_Click" Grid.Row="0" Grid.Column="3"/>

                            <!-- Subscription Field -->
                            <TextBlock Text="Select the subscription:" FontWeight="Bold" Margin="0,20,10,0" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
                            <ComboBox x:Name="SubscriptionPicker" SelectionChanged="SubscriptionPicker_SelectedIndexChanged" Background="White" Foreground="{StaticResource TextColor}" Width="360" Grid.Row="1" Grid.Column="1" Margin="0,20,0,0" IsEditable="True"/>
                            <TextBlock x:Name="SubscriptionInfoLabel" Grid.Row="1" Grid.Column="2" Foreground="{StaticResource TextColor}" />

                            <!-- Resource Group Field -->
                            <TextBlock Text="Select the Resource Group:" FontWeight="Bold" Margin="0,20,10,0" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                            <ComboBox x:Name="ResourceGroupPicker" IsEnabled="False" SelectionChanged="ResourceGroupPicker_SelectedIndexChanged" Background="White" Foreground="{StaticResource TextColor}" Width="360" Grid.Row="2" Grid.Column="1" Margin="0,20,0,0" IsEditable="True"/>
                            <TextBlock x:Name="ResourceGroupNameInfoLabel" Grid.Row="2" Grid.Column="2" Foreground="{StaticResource TextColor}" />

                            <!-- Azure Migrate Project Field -->
                            <TextBlock Text="Select the Azure Migrate Project:" FontWeight="Bold" Margin="0,20,10,0" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0"/>
                            <ComboBox x:Name="AzureMigrateProjectPicker" IsEnabled="False" SelectionChanged="AzureMigrateProjectPicker_SelectedIndexChanged" Background="White" Foreground="{StaticResource TextColor}" Width="360" Grid.Row="3" Grid.Column="1" Margin="0,20,0,0" IsEditable="True"/>
                            <TextBlock x:Name="AzureMigrateProjectNameInfoLabel" Grid.Row="3" Grid.Column="2" Foreground="{StaticResource TextColor}" />
                            
                             <!--Site Discovery Status Name Info Label--> 
                            <TextBlock x:Name="SiteDiscoveryStatusInfoLabel" Grid.Row="3" Grid.Column="2" Margin="10,26,0,0" Foreground="{StaticResource TextColor}"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Empty row to push content down -->
        <Grid Grid.Row="2" Height="50"/>
    </Grid>
</Page>
