<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AzureMigrateExplore.TrackProgress"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureMigrateExplore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="White">

    <Page.Resources>
        <!-- Define colors using SolidColorBrush -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#0078D7"/>
        <SolidColorBrush x:Key="AccentColor" Color="#32CD32"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="CardBackgroundColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#000000"/>
        <SolidColorBrush x:Key="DarkGrayColor" Color="#A9A9A9"/>
        <SolidColorBrush x:Key="ProgressBarColor" Color="#FF4500"/>

        <!-- Define styles -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundColor}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Shadow">
                <Setter.Value>
                    <ThemeShadow/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Fancy ProgressBar Style -->
        <Style x:Key="FancyProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="{StaticResource ProgressBarColor}"/>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Track Progress Card -->
        <!--
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <TextBlock Text="Track Progress" FontWeight="Bold" FontSize="16" Foreground="{StaticResource PrimaryColor}"/>
        </Border>-->

        <!-- Form Fields in Card-Style Container -->
        <Grid Grid.Row="1" Margin="10">
            <Border Style="{StaticResource CardStyle}">
                <StackPanel Orientation="Vertical">
                    <!-- Progress Card -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Progress" FontWeight="Bold" FontSize="14" Margin="0,5,10,0" Foreground="{StaticResource TextColor}"/>
                            <ProgressBar x:Name="ProgressBar" Width="950" Style="{StaticResource FancyProgressBarStyle}" Margin="60,0,0,20"/>
                        </StackPanel>
                    </Border>

                    <!-- Process Logs Card -->
                    <Border Style="{StaticResource CardStyle}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Process Logs" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="{StaticResource TextColor}"/>
                            <TextBox x:Name="ProcessLogsTextBox" TextWrapping="Wrap" Height="450" Foreground="Black" IsReadOnly="True" AcceptsReturn="True" TextChanged="TextBox_TextChanged"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>


